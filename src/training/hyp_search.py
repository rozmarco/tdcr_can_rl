import torch

# TODO: Search space: alpha, learning rate, batch_size, network width/depth

class HyperparameterSearch:
    def __init__(self, model, dataset, search_space, device=None):
        super().__init__()
        pass